# O.R.C.A. UI 浏览器渲染审查（2026-09-08）

## 结论

本次是只读浏览器审查，不是发布验收。Modern/Classic 的 `760px+` 主布局与 `UpdateBanner` 注入错误态通过了本轮断言；Classic `320-380px` 的重叠属于产品原生窗口不可达的浏览器超范围风险，不改变 Classic 旧基线，也不应被解读为 `760px+` 以外的实现承诺。

## 范围与边界

- 工作树：`D:\AI-Reasonix\.tmp\v2.1.3-worktree`
- 只复用了审查脚本的副本：
  - `.tmp/qa-2026-09-08/final-layout.cjs`，来源 `.tmp/release-2026-09-07/final-layout.cjs`
  - `.tmp/qa-2026-09-08/browser-audit.cjs`，来源 `.tmp/repair-2026-09-07/browser-audit.cjs`
- 新增 `.tmp/qa-2026-09-08/update-banner-final.cjs`，只注入合成的中文错误文本和更新状态。
- 未修改 `desktop/` 或其他源码；未调用真实 Provider、真实更新源、MCP、原生工具链，也未读取用户图片。
- `mock=fresh`、`mock=demo` 和 `mock=running` 仅用于浏览器布局状态。结果不能证明真实 Provider、Wails 原生窗口、系统 DPI、自动更新下载或本地 AI 已完成验证。
- 本轮使用浏览器 `deviceScaleFactor` 模拟 DPR `1/1.25/1.5/2`；这是 Playwright 的渲染参数，不是 Windows 系统 DPI 测试。

## 环境与服务

使用独立 Vite 端口 `43123`，未占用其他监听端口：`http://127.0.0.1:43123/`。

Vite 日志显示 `VITE v6.4.3 ready`。本轮服务监听进程 PID `16520`，测试结束后已停止；复查结果为 `port_43123_listening=False`。首次尝试使用系统 Node `18.20.0` 被 Playwright 的 Node 20 要求拒绝，随后改用工作区已提供的 Node 运行时，未改变被测源码或页面条件。

## 测试结果

### 主布局矩阵

`.tmp/qa-2026-09-08/final-layout.cjs` 完成 `160` 个组合，覆盖：

- Modern、Classic
- fresh 空态、running 对话态
- 侧栏打开与关闭
- 视口宽度 `1920/1366/1024/820/760`
- 浏览器模拟 DPR `1/1.25/1.5/2`

结果：`errors=[]`、`failures=[]`。在 fresh、侧栏关闭、DPR 1 的五个宽度上，Modern 与 Classic 的关键值均为：

- `welcomeCenterDelta=0`：空态内容相对主区域水平居中。
- `scrollRightDelta=0`：关闭右侧栏时主内容右边缘贴齐可视区域右侧。
- `actionsRightInset=1px`：Composer 操作区右对齐，保留 1px 内缩。
- `overflow=0`、`overlaps=0`、`clipped=0`。

补充的 `.tmp/qa-2026-09-08/fullscreen-empty-audit.cjs` 完成 `10` 个 Modern/Classic × `1920/1366/1024/820/760px` 组合，无脚本错误。水平中心偏差均为 `0px`；相对主区域的垂直中心偏差为 Modern `-5.93px` 至 `-6.01px`、Classic `-2px`。因此空态在主内容区域内水平居中，且仅有轻微的视觉上移，不能表述为几何意义上的双轴精确居中。

最终运行态截图与可核验结果（仅反馈两张截图）：

- [Modern 1366px running](2026-09-08-ui-evidence/modern-final-1366.png)
- [Classic 1366px running](2026-09-08-ui-evidence/classic-final-1366.png)
- [主布局 results.json](2026-09-08-ui-evidence/results.json)

### 窄窗口与交互覆盖

`.tmp/qa-2026-09-08/browser-audit.cjs` 完成 `78` 个报告项，另覆盖设置页 `52` 页，焦点审查 `4` 组；脚本级错误为 `0`。焦点结果均保持在 `role=dialog`、`aria-modal=true` 的对话框内，`escaped=0`。

发现 `12` 个带重叠报告项，全部属于 Classic：

- 中文 `380/360/320px` 的 fresh、running、demo：顶栏“搜索会话、命令、文件…”与“展开工作区”重叠。
- 中文 `380/360/320px` 的 running：Composer 左侧“协作方式”、工具权限、模型/模式/更多控制等控件互相挤压。
- 英文 `380px` 的 fresh、running、demo：顶栏搜索框与“展开工作区”重叠；running 另有 Composer 左侧控件挤压。

`desktop/main.go:120` 的实际实现将 `MinWidth` 设为 `760`。因此上述 `320-380px` 只存在于浏览器模拟，是产品原生窗口不可达的超范围风险，不是要求本补丁修改 Classic 布局。本轮保留 Classic 旧基线，未调整源码。`760px+` 的矩阵通过不掩盖这一模拟缺口，也不证明浏览器之外的原生窗口行为。

### UpdateBanner 长中文错误态

最后使用新 Vite 端口 `43124` 的 `.tmp/qa-2026-09-08/update-banner-final.cjs` 重跑，完成 `12` 个组合：Modern/Classic × `1366/760/580/460/380/320px`。

该轮监听进程 PID `85196` 已停止；复查结果为 `port_43124_listening=False`。

注入错误文本为一段包含网络、签名清单、安装包校验、代理、磁盘空间、权限、重试和手动下载提示的长中文错误；按钮为“重试”。结果：`failures=[]`、`errors=[]`，所有组合均满足：

- 横向 `overflow=0`。
- 错误文本可见且完整存在于状态区域。
- 按钮可见、标签为“重试”、未裁切。
- 状态区与操作区、正文与操作区没有重叠。
- `320px` 下横幅仍会换行并把操作区置于下一行，按钮仍在横幅内部。

这是合成状态注入测试，不代表真实 updater 请求、签名校验、下载或安装行为已经验证。

## 需要保留的限制

本报告没有把浏览器 mock 的通过结果扩大为“所有功能通过”。未在本轮确认真实 Provider、图片读写、Artifacts、工程工具、Subagents、MCP/Skill、Bots/Automation/Memory、Local AI、Computer Control、权限隔离、安装包、更新源或原生窗口行为。尤其是 Classic `320-380px` 的重叠是产品原生窗口不可达的浏览器超范围风险；本补丁保留 Classic 旧基线，后续只需在产品支持范围或相关 UI 变更时重新审查。
